<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
  <link rel="stylesheet" href="/assets/stylesheets/main.css">
</head>
<body>
  <header>
    <% include partials/_header %>
  </header>
    <h1>Your URL's</h1>
      <div class="box">
      <ul>
        <% for (var url in urls[user['id']]) { %>
        <li><%= url %> - <%= urls[user['id']][url] %>

        <form method="POST" action="/urls/<%= url %>/delete">
        <input type="submit" value="Delete"></form>

        <a href = "/urls/<%= url %>">Edit</a>

        </li>
        <% } %>
      </ul>
      <p><a class="homelink" href="urls/new">Shorten another URL</a></p></div>
</body>
</html>
